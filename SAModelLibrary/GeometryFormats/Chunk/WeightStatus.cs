namespace SAModelLibrary.GeometryFormats.Chunk
{
    /// <summary>
    /// Indicates which calculation phase the weight data belongs to.
    /// </summary>
    public enum WeightStatus : byte
    {
        Start,
        Middle,
        End
    }
}